# Image To Dot Matrix

This is HTML5 Canvas program written a long long time ago when I was still new
to JavaScript. The script converts an image to a series of colored dots.

How to See Demo:
1. `yarn install`
2. `yarn start`
3. Open browser to [http://localhost:8080](http://localhost:8080)

